# pgAdmin Setup (Sleekbook)

Installed via pip in a virtual environment (the apt version is broken/flickering).

Location: ~/pgadmin-env

Launch:
    source ~/pgadmin-env/bin/activate
    pgadmin4

Access: http://127.0.0.1:5050

Verified working 2026-07-04: connected to NUC's labdb, confirmed live
query against sales_raw table (5 rows returned).
